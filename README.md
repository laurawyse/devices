# Rails app for managing device readings

## Getting Started

Install ruby version

```
rbenv install
```

Install dependencies

```
bundle install
```

Run the server:

```
rails server
```

Run the tests:

```
rspec
```